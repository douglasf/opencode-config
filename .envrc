# THIS FILE SHOULD BE DELETED — it does not belong here.
# The .envrc should be in your *project* directory, not in the opencode config dir.
# See README.md "Enabling Websearch" for setup instructions.
#
# To clean up: rm ~/.config/opencode/.envrc && git add -A && git commit
